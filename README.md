# Ticket Analysis Agent with Frontend

## Setup
1. Install requirements: `pip install -r requirements.txt`
2. Update `config/config.yaml` with LLaMA2 model path
3. Create uploads directory: `mkdir -p data/uploads`

## Running
```bash
python app.py